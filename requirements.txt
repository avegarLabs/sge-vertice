# =====================================================================
# sge-vertice — runtime dependencies (Python 3.7 / Django 2.0)
# =====================================================================

# ---- Core framework -------------------------------------------------
django==2.0.2
django-auditlog==0.4.7
django-filter==2.0.0
django-widget-tweaks==1.4.3
django-highcharts==0.1.7

# ---- App-level deps imported in code (previously missing) -----------
django-decorator-include==2.0
django-select2==6.3.1
# django-auditlog 0.4.7 has a strict pin to python-dateutil==2.6.0;
# we keep that exact version because relativedelta (the only API used
# in this codebase) is present since dateutil 2.5.
python-dateutil==2.6.0

# ---- Database driver ------------------------------------------------
psycopg2==2.7.7

# ---- Reporting / PDF ------------------------------------------------
Pillow==6.2.0
PyPDF2==1.26.0
reportlab==3.5.28
xhtml2pdf==0.2.3
# pisa==3.0.33  # DEPRECATED legacy package (Py2-only, 2010). The `pisa`
#               # symbol used in the code is `from xhtml2pdf import pisa`,
#               # a submodule of xhtml2pdf — no separate package needed.

# ---- Misc utilities -------------------------------------------------
html5lib==1.0.1
jdcal==1.4
pytz==2018.9
six==1.12.0
webencodings==0.5.1

# ---- Production WSGI server ----------------------------------------
gunicorn==20.1.0

# ---- Testing (kept for compatibility with existing test runner) ----
django-nose==1.4.6
nose==1.3.7
